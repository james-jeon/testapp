import React from 'react';
import { View } from 'react-native';
import 'TodoTemplate.scss';

const TodoTemplate = () => {
    return (
        <View className={TodoTemplate}>

        </View>
    );
}

const styles = StyleSheet.create({

});

export default TodoTemplate;